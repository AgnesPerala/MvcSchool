@model IEnumerable<School.Models.StudentClass>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.TeacherName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StudentClassId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StudentClassId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StudentClassId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
